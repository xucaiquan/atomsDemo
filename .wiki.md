# Project Summary

Atoms Studio 是一个面向应用原型和业务页面生成的智能应用开发平台。用户通过自然语言描述需求，系统自动完成需求分析、页面结构设计和 HTML 代码生成，并在隔离沙箱中预览可交互应用。项目、版本、生成步骤和对话记录会持久化保存，支持历史版本恢复和持续迭代。

系统采用异步三阶段生成流程：请求先创建版本和步骤并立即返回，由后台任务执行生成，前端轮询步骤状态并加载结果。当前支持历史需求上下文、指代消解、两轮增量生成、代码续写、整篇重跑、HTML 校验、版本切换、回滚恢复、任务取消、错误分类、流水线恢复和 stale 任务处理。

项目支持登录用户及匿名访客。私有项目按 `owner_key` 隔离，演示项目全员可见但只读；匿名身份通过 HMAC 签名 Cookie 维持。生成任务具备取消、恢复、心跳和状态修复能力，避免旧任务或旧会话覆盖最新结果。

# Project Module Description

- **异步智能生成流水线**
  - 通过 AI Hub 执行需求分析、结构设计和 HTML 生成。
  - 预创建版本与步骤后异步执行，前端轮询步骤状态。
  - 自动注入历史需求、版本摘要和上一版 HTML。
  - 支持上下文截断、指代消解、两轮增量、代码续写和整篇重跑。
  - 在阶段边界、模型调用期间和任务收尾阶段检查取消状态。
  - 支持任务恢复、心跳、运行状态修复和 stale 任务清理。

- **任务取消与错误处理**
  - 提供版本级取消接口，先持久化取消状态，再终止后台任务。
  - 模型等待期间支持中断，迟到结果不会覆盖已取消任务。
  - 统一分类 AI Hub 错误并返回稳定的 `error_type`。
  - 前端展示失败原因，并根据任务状态控制输入、停止和重试操作。

- **项目归属与访问隔离**
  - 使用 `owner_key` 标识登录用户或匿名访客。
  - 私有项目仅允许所属身份访问，其他身份按资源不存在处理。
  - `is_demo=true` 的演示项目对所有身份可见，但禁止写操作。
  - OwnerContext 和统一依赖用于过滤项目、版本、消息和生成步骤。
  - 支持将无归属历史项目幂等回填为只读演示项目。

- **认证与访客身份**
  - 支持登录、认证回调、登出和受保护路由。
  - 复用平台 JWT 解析及认证 SDK。
  - 未登录用户通过 HMAC 签名 Cookie 获得稳定匿名归属。
  - 前端统一处理加载中、已登录和未登录状态。

- **上下文记忆与版本摘要**
  - 持久化应用名称、功能要点和结构设计摘要。
  - 历史需求参与后续生成，支持“继续刚才的需求”等指代表达。
  - 对话和生成步骤同步落库。
  - 支持增量版本恢复及恢复后的 HTML 内容一致性校验。

- **项目与版本管理**
  - 管理项目、版本、生成步骤和消息记录。
  - 支持项目列表、版本切换、历史内容恢复和回滚。
  - 使用更新时间和内容哈希辅助判断最新版本及恢复结果。
  - 在界面中明确展示演示项目的只读状态。

- **工作区**
  - 三栏布局展示项目列表、智能体步骤与对话、源码及应用预览。
  - 支持步骤状态展示、步骤回放、暂停、重置、停止和恢复。
  - 展示 `pending`、`running`、`succeeded`、`failed`、`cancelled` 等状态。
  - 根据任务状态禁用冲突操作并展示失败原因。

- **沙箱预览**
  - 提取完整 HTML，校验文档结构并注入 CSP 后通过 iframe 渲染。
  - iframe 使用 `allow-scripts allow-forms`，不授予 `allow-same-origin`。
  - 对截断内容、缺少文档结构和无效模型输出返回明确错误。

- **后端 API**
  - 提供健康检查、认证、项目、版本、消息、生成步骤、设置、存储和 AI Hub 接口。
  - 支持步骤查询、版本恢复、生成取消和归属过滤。
  - 统一处理参数校验、资源不存在、权限隔离、演示项目只读、生成失败和取消冲突。
  - 步骤响应包含前端需要的 `error_type` 等派生字段。

- **数据、迁移与脚本**
  - 使用 ORM 管理项目、版本、消息和生成步骤等实体。
  - 使用 Alembic 管理数据库迁移。
  - 提供演示项目归属回填脚本及项目数据模型 JSON。
  - 后台任务使用独立数据库会话。
  - 提供 AI Hub 探测、端到端验收和两轮生成验证脚本。

- **自动化测试与验收**
  - 覆盖 HTML 提取、结构校验、截断处理和 CSP 注入。
  - 覆盖上下文拼接、指代消解、截断和代码续写。
  - 覆盖归属依赖、双会话隔离、演示项目可见性和只读行为。
  - 覆盖流水线恢复、取消、心跳、stale 状态、两轮增量和版本恢复。
  - 提供测试夹具、FakeAIHub、内联生成测试和端到端验收脚本。

# Directory Tree

```text
app/
├── .mgx/
│   └── config.yaml                                  # 后端工程配置
├── CLAUDE.md                                        # 项目协作与开发约定
├── backend/
│   ├── main.py                                      # FastAPI 应用入口
│   ├── lambda_handler.py                            # Serverless 处理入口
│   ├── e2e_acceptance.py                            # 端到端验收脚本
│   ├── e2e_two_round.py                             # 两轮生成验证脚本
│   ├── probe_upstream.py                            # 上游 AI 服务探测脚本
│   ├── requirements.txt                             # 后端依赖
│   ├── requirements.default                         # 默认依赖模板
│   ├── pytest.ini                                   # pytest 测试配置
│   ├── alembic.ini                                  # 数据库迁移配置
│   ├── alembic/                                     # Alembic 环境和迁移版本
│   ├── core/                                        # 配置、认证、数据库、枚举和遥测
│   ├── dependencies/
│   │   └── owner.py                                 # 用户及匿名访客归属解析
│   ├── middlewares/                                 # FastAPI 中间件
│   ├── models/                                      # ORM 数据模型
│   ├── schemas/                                     # API 请求与响应结构
│   ├── routers/
│   │   └── atoms.py                                 # 生成、步骤、恢复和取消接口
│   ├── services/
│   │   ├── pipeline.py                              # 生成、恢复、增量和 stale 控制
│   │   ├── prompts.py                               # 历史需求和生成提示词
│   │   ├── aihub.py                                 # AI Hub 调用封装
│   │   ├── aihub_errors.py                          # AI Hub 错误分类
│   │   ├── html_extract.py                          # HTML 提取、校验和 CSP 注入
│   │   └── *.py                                     # 项目、版本、消息、步骤等业务服务
│   ├── data_models/
│   │   └── projects.json                             # 项目数据模型定义
│   ├── mock_data/                                   # 演示种子数据
│   ├── scripts/
│   │   └── backfill_demo_owner.py                   # 演示项目归属回填脚本
│   ├── skills_docs/                                 # AI、对象存储和 SDK 文档
│   ├── tests/
│   │   ├── conftest.py                              # 测试环境和数据库夹具
│   │   ├── fakes.py                                 # FakeAIHub 等测试替身
│   │   ├── test_conftest_smoke.py                   # 测试夹具冒烟测试
│   │   ├── test_fake_aihub.py                       # FakeAIHub 行为测试
│   │   ├── test_generation_inline.py                # 内联生成测试
│   │   ├── test_html_extract.py                     # HTML 处理测试
│   │   ├── test_context_memory.py                   # 上下文记忆与续写测试
│   │   ├── test_owner_dependency.py                 # 归属依赖测试
│   │   ├── test_owner_isolation.py                  # 归属隔离测试
│   │   ├── test_pipeline_recovery.py                # 流水线恢复测试
│   │   ├── test_stale_recovery.py                   # stale 任务恢复测试
│   │   ├── test_two_round_increment.py              # 两轮增量测试
│   │   └── test_version_restore.py                  # 版本恢复测试
│   └── utils/                                       # 日志等通用工具
├── frontend/
│   ├── package.json                                 # 前端依赖与脚本
│   ├── vite.config.ts                               # Vite 配置
│   ├── tailwind.config.ts                           # Tailwind 配置
│   ├── index.html                                   # Vite 前端 HTML 入口
│   ├── src/
│   │   ├── App.tsx                                  # 路由、认证门控和整体布局
│   │   ├── main.tsx                                 # React 启动入口
│   │   ├── pages/                                   # 工作区、认证和博客页面
│   │   ├── components/                              # 工作区、预览、对话和项目组件
│   │   ├── components/ui/                           # 通用 UI 组件
│   │   ├── contexts/
│   │   │   └── AuthContext.tsx                      # 认证状态和用户会话
│   │   ├── hooks/                                   # React Hooks
│   │   ├── lib/
│   │   │   ├── atoms.ts                             # 生成、轮询、恢复和取消 API
│   │   │   ├── auth.ts                              # 登录、登出和认证请求
│   │   │   └── constants.ts                         # 前端常量
│   │   ├── api/                                     # 设置接口封装
│   │   └── *.tsx                                    # 页面与组件实现
│   ├── public/                                      # 静态资源
│   └── prerender/                                   # 博客预渲染与站点地图生成
├── docs/
│   ├── superpowers/plans/                            # 实施计划
│   ├── superpowers/specs/                            # 技术规格说明
│   ├── 优化文档-无登录数据隔离与生成链路修复.md      # 无登录隔离和生成链路优化说明
│   └── 验收复核报告-生成流水线与归属隔离.md           # 流水线与归属隔离验收报告
├── start_app_v2.sh                                  # 应用启动脚本
├── uploads/
│   ├── specs/001-atoms-demo/                         # Demo 需求、接口和实施文档
│   ├── specs/002-harden-increment-session/           # 增量会话规格、契约和任务文档
│   └── 2026-09-20-atoms-studio-ownership-and-pipeline-hardening-design.md
│                                                       # 归属隔离与流水线加固设计
```

# File Description Inventory

| 范围 | 主要文件 | 用途 |
|---|---|---|
| 项目约定 | `CLAUDE.md` | 记录项目协作、开发和验证约定 |
| 归属解析 | `backend/dependencies/owner.py` | 解析 JWT、匿名身份和 OwnerContext |
| 生成路由 | `backend/routers/atoms.py` | 接收生成请求、执行归属过滤、查询步骤、恢复和取消任务 |
| 生成流水线 | `backend/services/pipeline.py` | 执行多阶段生成、增量处理、恢复、心跳和 stale 控制 |
| 生成提示词 | `backend/services/prompts.py` | 拼接历史需求、处理指代并约束三阶段输出 |
| AI 错误 | `backend/services/aihub_errors.py` | 归类 AI Hub 异常并生成稳定错误类型 |
| HTML 处理 | `backend/services/html_extract.py` | 提取完整 HTML、校验结构、处理截断并注入 CSP |
| 归属回填 | `backend/scripts/backfill_demo_owner.py` | 将无归属历史项目幂等标记为只读演示项目 |
| 端到端验证 | `backend/e2e_acceptance.py`、`backend/e2e_two_round.py` | 验证完整生成链路和两轮增量行为 |
| 上游探测 | `backend/probe_upstream.py` | 检查上游 AI 服务可用性 |
| 前端认证 | `frontend/src/contexts/AuthContext.tsx`、`frontend/src/lib/auth.ts` | 管理用户会话、登录、登出和认证状态 |
| 前端 API | `frontend/src/lib/atoms.ts` | 封装生成受理、步骤轮询、版本恢复和取消请求 |
| 主工作区 | `frontend/src/pages/Index.tsx` | 管理生成轮询、恢复、停止和失败展示 |
| 需求输入 | `frontend/src/components/PromptInput.tsx` | 输入需求并根据任务状态控制提交或停止 |
| 版本管理 | `frontend/src/components/VersionSwitcher.tsx` | 切换、恢复版本并展示演示项目只读状态 |
| 源码查看 | `frontend/src/components/CodeViewer.tsx` | 展示生成源码及版本内容 |
| 数据访问 | `backend/models/*.py`、`backend/services/database.py` | 定义实体、归属字段和数据库操作 |
| 测试基础设施 | `backend/tests/conftest.py`、`backend/tests/fakes.py` | 提供 SQLite、认证环境和 FakeAIHub 测试支持 |
| 归属测试 | `backend/tests/test_owner_dependency.py`、`test_owner_isolation.py` | 验证归属解析、私有项目隔离、演示可见和只读行为 |
| 流水线测试 | `backend/tests/test_pipeline_recovery.py`、`test_stale_recovery.py` | 验证中断恢复、心跳和过期任务修复 |
| 增量与恢复测试 | `backend/tests/test_two_round_increment.py`、`test_version_restore.py` | 验证两轮增量、回滚及内容一致性 |
| 上下文测试 | `backend/tests/test_context_memory.py` | 验证历史需求、指代消解、截断和续写 |
| 测试替身 | `backend/tests/test_fake_aihub.py`、`test_generation_inline.py` | 验证模型替身和内联生成流程 |
| 项目文档 | `docs/`、`uploads/` | 记录规格、实施计划、契约、研究和验收结果 |

# Technology Stack

- **后端**：Python、FastAPI、SQLAlchemy、Alembic、Pydantic、asyncio 后台任务
- **AI 能力**：AI Hub、DeepSeek、多阶段需求分析与 HTML 代码生成
- **前端**：React、TypeScript、Vite
- **样式与组件**：Tailwind CSS、shadcn 风格 UI、PostCSS
- **应用预览**：受 CSP 约束的 sandbox iframe
- **数据存储**：关系型数据库、ORM、Alembic、对象存储
- **认证安全**：JWT、HMAC 匿名 Cookie、认证 SDK、受保护路由
- **任务控制**：数据库任务状态、后台任务、取消、心跳、恢复和 stale 检测
- **上下文管理**：历史需求摘要、指代消解、版本摘要和增量上下文
- **版本一致性**：版本更新时间、回滚恢复、SHA-256 内容校验
- **输出可靠性**：HTML 完整性校验、截断续写、整篇重跑和错误分类
- **测试与质量**：pytest、SQLite 测试库、FakeAIHub、端到端验收、ESLint、TypeScript 构建检查
- **部署入口**：Uvicorn 开发运行、Serverless Lambda 入口

# Usage

## 后端安装与运行

```bash
cd app/backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
alembic upgrade head
uvicorn main:app --reload
```

Windows 激活虚拟环境：

```bash
.venv\Scripts\activate
```

根据运行环境配置数据库、JWT、AI Hub、对象存储等必要环境变量。测试环境需提供 JWT 相关配置；首次运行时可按项目需要加载 `mock_data` 中的演示数据。

## 演示项目归属回填

```bash
cd app/backend
python scripts/backfill_demo_owner.py
```

脚本会将无 `owner_key` 的历史项目幂等标记为 `is_demo=true`，并保持演示项目只读。

## 前端安装与运行

```bash
cd app/frontend
npm install
npm run dev
```

## 前端检查与构建

```bash
npm run lint
npm run build
```

## 后端测试

```bash
cd app/backend
pytest
```

## 端到端验收与上游探测

```bash
cd app/backend
python e2e_acceptance.py
python e2e_two_round.py
python probe_upstream.py
```

运行前需准备对应数据库、认证和 AI Hub 环境变量。

## 一键启动

```bash
cd app
bash start_app_v2.sh
```

启动后可体验项目归属隔离、演示项目只读、异步生成、历史需求迭代、任务取消与恢复、stale 任务处理、版本回滚、对话和沙箱预览。
