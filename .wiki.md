# Project Summary

Atoms Studio 是一个面向应用原型与业务页面生成的智能应用开发平台。用户通过自然语言描述需求，系统依次完成需求分析、结构设计和代码生成，并在隔离沙箱中预览可交互应用；项目、版本、生成步骤及对话记录会持久化保存，支持基于历史版本继续迭代。

当前 Demo 已完成端到端 AI 生成流程、三栏工作区、版本切换、步骤回放、对话记录、沙箱预览及自动化验证。

# Project Module Description

- **智能生成流水线**
  - 通过 AI Hub 执行需求分析、结构设计、HTML 代码生成三阶段流程。
  - 支持基于已有版本追加需求生成新版本。
  - 代码生成支持较大输出预算及空内容重试。
- **项目与版本管理**
  - 管理项目、版本、生成步骤和消息记录。
  - 支持项目列表、版本切换和历史内容恢复。
  - 已包含演示数据：3 个项目、4 个版本、8 条消息、12 个步骤。
- **工作区**
  - 三栏布局展示项目列表、智能体步骤与对话、代码及应用预览。
  - 支持步骤状态展示、步骤回放、暂停和重置。
- **沙箱预览**
  - 将生成的 HTML 自动提取、校验并注入 CSP 后，通过 iframe 渲染。
  - iframe 使用 `allow-scripts allow-forms`，不授予 `allow-same-origin`。
- **认证与用户**
  - 提供认证回调、登出回调、受保护路由及用户接口。
- **后端 API**
  - 提供健康检查、认证、项目、版本、消息、生成步骤、设置、存储及 AI Hub 接口。
  - 统一处理 400/404 错误响应。
- **数据与迁移**
  - 使用 ORM 模型管理业务实体。
  - 使用 Alembic 管理数据库迁移。
  - 提供 mock 数据和数据模型 JSON 文件。
- **自动化测试**
  - `test_html_extract.py` 覆盖 HTML 提取、截断校验、CSP 注入、错误场景等 17 项测试。

# Directory Tree

```text
app/
├── .mgx/
│   └── config.yaml                 # 后端工程配置
├── backend/
│   ├── main.py                     # FastAPI 应用入口
│   ├── lambda_handler.py           # Serverless 处理入口
│   ├── requirements.txt            # 后端依赖
│   ├── requirements.default        # 默认依赖模板
│   ├── alembic.ini                 # 数据库迁移配置
│   ├── alembic/                    # Alembic 环境、模板和迁移版本
│   ├── core/                       # 配置、认证、数据库、枚举、加密和遥测
│   ├── dependencies/               # 认证与数据库依赖注入
│   ├── middlewares/                # FastAPI 中间件
│   ├── models/                     # ORM 数据模型
│   ├── schemas/                    # API 请求与响应结构
│   ├── routers/                    # REST API 路由
│   ├── services/                   # 业务服务、AI 流程、存储、支付和数据处理
│   ├── data_models/                # 数据结构定义 JSON
│   ├── mock_data/                  # 演示种子数据
│   ├── skills_docs/                # AI、对象存储和 SDK 使用说明
│   ├── tests/                      # 后端自动化测试
│   └── utils/                      # 日志等通用工具
├── frontend/
│   ├── package.json                # 前端依赖与脚本
│   ├── vite.config.ts              # Vite 配置
│   ├── tailwind.config.ts          # Tailwind 配置
│   ├── index.html                  # 前端 HTML 入口
│   ├── src/
│   │   ├── App.tsx                 # 应用路由与整体布局
│   │   ├── main.tsx                # React 启动入口
│   │   ├── pages/                  # 工作区、认证和博客页面
│   │   ├── components/             # 工作区、预览、对话和项目组件
│   │   ├── components/ui/          # 通用 UI 组件
│   │   ├── contexts/               # 认证上下文
│   │   ├── hooks/                  # React Hooks
│   │   ├── lib/                    # API、认证、配置和工具函数
│   │   └── api/                    # 设置接口封装
│   ├── public/                     # favicon、robots 等静态资源
│   ├── prerender/                  # 博客预渲染与站点地图生成
│   └── *.json/*.js/*.ts            # TypeScript、ESLint、PostCSS 等工程配置
├── start_app_v2.sh                 # 应用启动脚本
└── uploads/
    ├── specs/001-atoms-demo/       # Demo 需求、接口、数据模型和实施文档
    └── tasks.md                    # 项目任务记录
```

# File Description Inventory

| 范围 | 主要文件 | 用途 |
|---|---|---|
| 后端入口 | `backend/main.py`、`lambda_handler.py` | 创建 Web 应用及提供 Serverless 入口 |
| 核心基础设施 | `backend/core/*.py` | 环境配置、数据库、认证、加密、枚举和请求遥测 |
| 数据访问 | `backend/models/*.py`、`backend/services/database.py` | 定义实体并封装数据库操作 |
| API 层 | `backend/routers/*.py`、`backend/schemas/*.py` | 暴露业务接口并校验数据契约 |
| AI 生成 | `services/aihub.py`、`pipeline.py`、`prompts.py` | 调用 AI Hub、执行三阶段生成和组织提示词 |
| 内容处理 | `services/html_extract.py`、`storage.py` | 提取完整 HTML、注入 CSP 并处理对象存储 |
| 业务服务 | `services/projects.py`、`versions.py`、`messages.py`、`generation_steps.py`、`user.py` | 管理项目、版本、消息、步骤和用户 |
| 数据资源 | `data_models/*.json`、`mock_data/*.json` | 定义数据结构及初始化演示数据 |
| 前端页面 | `pages/Index.tsx`、认证页面、博客页面 | 提供工作区、认证流程和博客内容 |
| 前端工作区 | `AgentSteps.tsx`、`ConversationPanel.tsx`、`PreviewPane.tsx`、`CodeViewer.tsx` | 展示生成进度、对话、代码和沙箱预览 |
| 前端交互 | `PromptInput.tsx`、`ProjectList.tsx`、`VersionSwitcher.tsx` | 输入需求、选择项目和切换版本 |
| 前端基础组件 | `components/ui/*.tsx` | 提供基于 shadcn 风格的通用界面组件 |
| 前端基础库 | `src/lib/*.ts`、`contexts/AuthContext.tsx` | API、认证、配置、状态和通用工具 |
| 测试 | `tests/test_html_extract.py` | 验证 HTML 提取和 CSP 处理逻辑 |
| 项目文档 | `uploads/specs/001-atoms-demo/*`、`skills_docs/*` | 记录需求、接口、实施计划、快速开始和能力说明 |

# Technology Stack

- **后端**：Python、FastAPI、SQLAlchemy、Alembic、Pydantic
- **AI 能力**：AI Hub，使用 DeepSeek 完成多阶段需求分析与代码生成
- **前端**：React、TypeScript、Vite
- **样式与组件**：Tailwind CSS、shadcn 风格 UI、PostCSS
- **应用预览**：受 CSP 约束的 sandbox iframe
- **数据存储**：关系型数据库、ORM 持久化、Alembic 迁移、对象存储服务
- **认证安全**：认证依赖、回调页面、加密及受保护路由
- **测试与质量**：pytest、ESLint、TypeScript 构建检查
- **部署入口**：Uvicorn 开发运行、Serverless Lambda 入口

# Usage

## 后端安装与运行

```bash
cd app/backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
alembic upgrade head
uvicorn main:app --reload
```

Windows 激活虚拟环境可使用：

```bash
.venv\Scripts\activate
```

根据运行环境配置数据库、认证、AI Hub、对象存储等必要环境变量。首次运行时按项目需要加载 `mock_data` 中的演示数据。

## 前端安装与运行

```bash
cd app/frontend
npm install
npm run dev
```

## 前端检查与构建

```bash
npm run lint
npm run build
```

## 后端测试

```bash
cd app/backend
pytest
```

## 一键启动

```bash
cd app
bash start_app_v2.sh
```

启动后可进入 App Viewer，选择项目体验项目列表、生成步骤、对话、版本切换和沙箱预览。
