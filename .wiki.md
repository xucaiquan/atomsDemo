# Project Summary

该项目是一个每日饮食食谱推荐应用，支持按周期生成多餐食谱，并在 App Viewer 中展示推荐页面。系统通过多阶段生成流程产出食谱内容与配图，支持失败状态提示和重试。

本次变更解决了第二轮增量生成因逐道生成内联 SVG 导致阶段三超时的问题，将配图改为固定的轻量渲染方案，降低生成代码量并保持既有超时预算不变。

# Project Module Description

- **食谱生成模块**：按阶段生成每日、多餐食谱及页面代码。
- **阶段控制与超时模块**：管理生成阶段预算、重试次数和失败状态。
- **配图生成模块**：使用渐变色块、Emoji 和菜品配色数据生成轻量配图，避免逐道手绘 SVG。
- **数据库诊断模块**：查询食谱生成版本、阶段状态、时间戳和失败记录，定位卡住或超时任务。
- **测试模块**：验证阶段三提示词包含轻量配图约束，并执行后端回归测试。

# Directory Tree

```text
app/
└── backend/
    ├── services/
    │   └── prompts.py
    │       阶段生成提示词，包含轻量配图规则
    ├── diagnose_recipe.py
    │   食谱项目版本与生成状态诊断脚本
    ├── diagnose_stuck.py
    │   卡住任务及阶段时间戳诊断脚本
    └── tests/
        └── test_lightweight_image_rule.py
            轻量配图提示词约束测试
```

# File Description Inventory

| 文件 | 类型 | 说明 |
|---|---|---|
| `app/backend/services/prompts.py` | 修改 | 将阶段三的逐道内联 SVG 配图要求替换为通用轻量配图方案。 |
| `app/backend/diagnose_recipe.py` | 新增 | 查询食谱项目的版本、阶段和失败记录。 |
| `app/backend/diagnose_stuck.py` | 新增 | 定位数据库中的卡住任务及其停滞时长。 |
| `app/backend/tests/test_lightweight_image_rule.py` | 新增 | 锁定轻量配图规则，防止提示词回退。 |

# Technology Stack

- Python
- 后端分阶段生成流程
- 数据库会话管理
- 提示词驱动的代码生成
- pytest 自动化测试
- HTML/CSS/Emoji/渐变色块配图渲染

# Usage

## 安装依赖

在项目根目录按现有 Python 依赖配置安装后端依赖：

```bash
pip install -r requirements.txt
```

## 运行诊断

```bash
python app/backend/diagnose_recipe.py
python app/backend/diagnose_stuck.py
```

运行前需确保数据库配置和后端运行环境已正确设置。诊断脚本通过 `db_manager.session()` 读取任务状态、版本和阶段时间戳。

## 运行测试

```bash
pytest app/backend/tests/test_lightweight_image_rule.py
pytest
```

## 生成与验证

重新提交食谱配图请求后，在 App Viewer 刷新项目查看生成结果。若任务失败，页面应展示失败提示和重试入口。
