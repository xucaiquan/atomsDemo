# Project Summary

Atoms Studio 是一个面向应用原型和业务页面生成的智能应用开发平台。用户通过自然语言描述需求，系统完成需求分析、页面结构设计和 HTML 代码生成，并在隔离沙箱中预览可交互应用。项目、版本、生成步骤和对话记录会持久化保存，支持基于历史版本继续迭代。

系统采用异步生成流程：请求校验并预创建版本和步骤后立即返回，后台执行三阶段生成；前端轮询步骤状态并在完成后加载结果。当前支持需求历史上下文、指代消解、截断续写、整篇重跑、HTML 完整性校验、版本切换、步骤回放、对话记录、沙箱预览，以及生成任务中断和取消。

生成任务支持在阶段边界及模型调用期间取消，并通过独立的 `cancelled` 状态持久化结果，避免延迟返回覆盖取消状态。项目历史需求会自动注入分析、结构设计和代码生成阶段，帮助模型理解“继续刚才的需求”等迭代指令。

# Project Module Description

- **异步智能生成流水线**
  - 通过 AI Hub 执行需求分析、结构设计和 HTML 代码生成。
  - 生成请求预创建版本和步骤后异步执行，前端轮询状态。
  - 自动装配项目历史需求，最多保留 6 条、每条截断至 200 字符。
  - 对“继续、刚才、之前说的、再优化”等指代进行历史上下文消解。
  - 迭代时同时传入上一版完整 HTML，支持保留既有功能并叠加新需求。
  - 代码截断时支持断点续写，仍不完整时自动整篇重跑。
  - 在阶段边界、任务启动前和生成收尾处检查取消状态。

- **生成任务取消**
  - 提供版本级取消接口，先将任务状态持久化为 `cancelled`，再取消后台任务。
  - 正在等待模型响应时支持中断。
  - 已完成版本重复取消返回冲突响应；取消后可重新提交生成。
  - 延迟到达的成功或失败结果不会覆盖已取消状态。

- **上下文记忆与版本摘要**
  - 版本摘要持久化应用名称、功能要点和结构设计摘要。
  - 历史需求参与后续生成上下文拼接。
  - 对话记录与版本生成过程同步落库。

- **项目与版本管理**
  - 管理项目、版本、生成步骤和消息记录。
  - 支持项目列表、版本切换、历史内容恢复和进行中任务恢复。
  - 支持查看取消状态及重新发起生成。

- **工作区**
  - 三栏布局展示项目列表、智能体步骤与对话、代码及应用预览。
  - 支持步骤状态展示、步骤回放、暂停、重置和停止生成。
  - 展示 `pending`、`running`、`succeeded`、`failed`、`cancelled` 等状态。

- **沙箱预览**
  - 提取生成的完整 HTML，校验文档结构并注入 CSP 后通过 iframe 渲染。
  - iframe 使用 `allow-scripts allow-forms`，不授予 `allow-same-origin`。

- **认证与用户**
  - 提供认证回调、登出回调、受保护路由及用户接口。

- **后端 API**
  - 提供健康检查、认证、项目、版本、消息、生成步骤、设置、存储及 AI Hub 接口。
  - 提供版本步骤查询和生成取消接口。
  - 统一处理参数校验、资源不存在、生成失败、取消冲突和 HTML 不完整响应。

- **数据与迁移**
  - 使用 ORM 模型管理业务实体。
  - 使用 Alembic 管理数据库迁移。
  - 后台生成任务使用独立数据库会话。
  - 提供 mock 数据和数据模型 JSON 文件。

- **自动化测试**
  - HTML 提取测试覆盖结构校验、截断处理、CSP 注入和错误场景。
  - 新增上下文记忆测试，覆盖历史拼接、指代消解、截断和续写合并。
  - 后端单元测试、前端 lint、生产构建及 UI 检查均已通过。

# Directory Tree

```text
app/
├── .mgx/
│   └── config.yaml                 # 后端工程配置
├── backend/
│   ├── main.py                     # FastAPI 应用入口
│   ├── lambda_handler.py           # Serverless 处理入口
│   ├── requirements.txt            # 后端依赖
│   ├── requirements.default        # 默认依赖模板
│   ├── alembic.ini                 # 数据库迁移配置
│   ├── alembic/                    # Alembic 环境和迁移版本
│   ├── core/                       # 配置、认证、数据库、枚举和遥测
│   ├── dependencies/               # 认证与数据库依赖注入
│   ├── middlewares/                # FastAPI 中间件
│   ├── models/                     # ORM 数据模型
│   ├── schemas/                    # API 请求与响应结构
│   ├── routers/
│   │   └── atoms.py                # 生成、步骤查询和任务取消接口
│   ├── services/
│   │   ├── pipeline.py             # 三阶段生成、上下文、续写和取消控制
│   │   ├── prompts.py              # 历史需求拼接和生成提示词
│   │   ├── aihub.py                # AI Hub 调用封装
│   │   ├── html_extract.py         # HTML 提取、校验和 CSP 注入
│   │   └── *.py                    # 项目、版本、消息、步骤等业务服务
│   ├── data_models/                # 数据结构定义 JSON
│   ├── mock_data/                  # 演示种子数据
│   ├── skills_docs/                # AI、对象存储和 SDK 文档
│   ├── tests/
│   │   ├── test_html_extract.py    # HTML 处理测试
│   │   └── test_context_memory.py  # 上下文记忆与续写测试
│   └── utils/                      # 日志等通用工具
├── frontend/
│   ├── package.json                # 前端依赖与脚本
│   ├── vite.config.ts              # Vite 配置
│   ├── tailwind.config.ts          # Tailwind 配置
│   ├── index.html                  # Vite 前端 HTML 入口
│   ├── src/
│   │   ├── App.tsx                 # 应用路由与整体布局
│   │   ├── main.tsx                # React 启动入口
│   │   ├── pages/                  # 工作区、认证和博客页面
│   │   ├── components/             # 工作区、预览、对话和项目组件
│   │   ├── components/ui/          # 通用 UI 组件
│   │   ├── contexts/               # 认证上下文
│   │   ├── hooks/                  # React Hooks
│   │   ├── lib/                    # API、认证、配置和工具函数
│   │   └── api/                    # 设置接口封装
│   ├── public/                     # 静态资源
│   └── prerender/                  # 博客预渲染与站点地图生成
├── start_app_v2.sh                 # 应用启动脚本
└── uploads/
    ├── specs/001-atoms-demo/       # Demo 需求、接口和实施文档
    └── tasks.md                    # 项目任务记录
```

# File Description Inventory

| 范围 | 主要文件 | 用途 |
|---|---|---|
| 生成路由 | `backend/routers/atoms.py` | 接收生成请求、装配历史需求、注册后台任务并提供取消接口 |
| 生成流水线 | `backend/services/pipeline.py` | 执行三阶段生成、上下文传递、截断续写、重跑和取消状态控制 |
| 生成提示词 | `backend/services/prompts.py` | 定义需求历史拼接、指代消解及三阶段输出约束 |
| 上下文测试 | `backend/tests/test_context_memory.py` | 验证需求历史、指代消解、截断和续写合并逻辑 |
| HTML 测试 | `backend/tests/test_html_extract.py` | 验证 HTML 提取、完整性和 CSP 处理逻辑 |
| 后端入口 | `backend/main.py`、`lambda_handler.py` | 创建 Web 应用及提供 Serverless 入口 |
| AI 调用 | `backend/services/aihub.py` | 封装 AI Hub 请求及响应处理 |
| 核心基础设施 | `backend/core/*.py` | 环境配置、数据库、认证、加密和请求遥测 |
| 数据访问 | `backend/models/*.py`、`backend/services/database.py` | 定义实体并封装数据库操作 |
| 内容处理 | `backend/services/html_extract.py`、`storage.py` | 提取完整 HTML、校验文档结构、注入 CSP 并处理存储 |
| 业务服务 | `backend/services/projects.py`、`versions.py`、`messages.py`、`generation_steps.py`、`user.py` | 管理项目、版本、消息、步骤和用户 |
| 前端 API | `frontend/src/lib/atoms.ts` | 封装生成受理、步骤轮询、版本查询和取消请求 |
| 主工作区 | `frontend/src/pages/Index.tsx` | 管理生成、轮询、停止交互和取消终态 |
| 生成状态 | `frontend/src/components/AgentSteps.tsx` | 展示阶段进度及取消状态 |
| 项目管理 | `frontend/src/components/ProjectList.tsx` | 展示项目及生成任务状态 |
| 需求输入 | `frontend/src/components/PromptInput.tsx` | 输入需求并触发或停止生成 |
| 版本管理 | `frontend/src/components/VersionSwitcher.tsx` | 切换版本并展示取消状态 |
| 工作区组件 | `ConversationPanel.tsx`、`PreviewPane.tsx`、`CodeViewer.tsx` | 展示对话、代码和沙箱预览 |
| 项目文档 | `uploads/specs/001-atoms-demo/*`、`skills_docs/*` | 记录需求、接口、实施计划和能力说明 |

# Technology Stack

- **后端**：Python、FastAPI、SQLAlchemy、Alembic、Pydantic、asyncio 后台任务
- **AI 能力**：AI Hub、DeepSeek、多阶段需求分析与代码生成
- **前端**：React、TypeScript、Vite
- **样式与组件**：Tailwind CSS、shadcn 风格 UI、PostCSS
- **应用预览**：受 CSP 约束的 sandbox iframe
- **数据存储**：关系型数据库、ORM、Alembic、对象存储
- **认证安全**：认证依赖、回调页面、加密及受保护路由
- **任务控制**：数据库持久化任务状态、后台任务注册、取消和前端轮询
- **上下文管理**：历史需求摘要、指代消解、版本摘要持久化
- **输出可靠性**：HTML 完整性校验、截断续写、整篇重跑兜底
- **测试与质量**：pytest、ESLint、TypeScript 构建检查、UI 和端到端验证
- **部署入口**：Uvicorn 开发运行、Serverless Lambda 入口

# Usage

## 后端安装与运行

```bash
cd app/backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
alembic upgrade head
uvicorn main:app --reload
```

Windows 激活虚拟环境：

```bash
.venv\Scripts\activate
```

根据运行环境配置数据库、认证、AI Hub、对象存储等必要环境变量。首次运行时按项目需要加载 `mock_data` 中的演示数据。

## 前端安装与运行

```bash
cd app/frontend
npm install
npm run dev
```

## 前端检查与构建

```bash
npm run lint
npm run build
```

## 后端测试

```bash
cd app/backend
pytest
```

## 一键启动

```bash
cd app
bash start_app_v2.sh
```

启动后可进入 App Viewer，体验项目列表、历史需求迭代、异步生成、步骤轮询、停止生成、取消状态、对话、版本切换和沙箱预览。
