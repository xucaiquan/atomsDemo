# Project Summary

Atoms Studio 是一个面向应用原型和业务页面生成的智能开发平台。用户通过自然语言描述业务需求，系统自动完成需求分析、页面结构设计和 HTML 代码生成，并在隔离沙箱中预览可交互应用。项目、版本、生成步骤和对话记录会持久化保存，支持历史版本恢复、持续迭代和源代码预览。

系统采用异步三阶段生成流程，请求先创建版本和步骤，再由后台任务执行生成，前端轮询步骤状态并加载结果。生成链路支持历史需求上下文、指代消解、增量生成、代码续写、整篇重跑、HTML 校验、版本切换、回滚恢复、任务取消、失败原因展示、预算耗尽处理以及流水线异常恢复。

历史记忆会记录成功、失败和取消轮次。失败或取消轮次会携带状态标注进入需求历史，帮助“重新执行”“再试一次”等指代回到原始需求；`previous_html` 仍只取最近成功版本，避免失败产物污染增量基线。匿名访客和登录用户按 `owner_key` 隔离，演示项目全员可见但只读。

生成任务的 stale 判断已统一使用数据库写入侧的本地 naive 时间，并对缺失时间戳采取保守放行策略，避免新任务被错误回收。后端全量测试、前端 lint、TypeScript 类型检查和构建均已通过。

# Project Module Description

- **异步智能生成流水线**
  - 通过 AI Hub 执行需求分析、结构设计和 HTML 代码生成。
  - 生成请求预创建版本和步骤后异步执行，前端轮询步骤状态。
  - 自动注入项目历史需求和上一版成功 HTML。
  - 支持两轮增量生成、代码截断续写和不完整结果整篇重跑。
  - 支持生成预算、阶段超时、实时取消、任务恢复、心跳和 stale 状态清理。
  - stale 时间判断与数据库时间写入口径一致，缺失时间戳时不误回收新任务。

- **历史记忆与指代消解**
  - 成功、失败和取消轮次均进入需求历史，并附带状态标注。
  - “重新执行”“再试一次”等表达解释为重做原始需求，而不是生成任务清单。
  - `previous_html` 仅使用最近成功版本，失败或取消产物不作为增量基线。
  - 对历史上下文执行预算控制和长度截断。

- **增量生成与会话控制**
  - 维护生成会话和阶段状态，确保增量请求使用正确上下文。
  - 在阶段边界、模型调用期间和任务收尾阶段检查取消状态。
  - 取消优先于迟到的模型结果，避免已取消任务继续写入成功结果。
  - 区分预算耗尽、超时、取消和上游错误。

- **项目归属与隔离**
  - 使用 `owner_key` 标识登录用户或匿名访客归属。
  - 私有项目仅允许所属身份访问，其他身份返回资源不存在。
  - 演示项目对所有身份可见，但写操作返回冲突。
  - 通过 OwnerContext 和统一依赖过滤项目、版本、消息及生成步骤。

- **认证与访客身份**
  - 支持登录、认证回调、登出及受保护路由。
  - 复用平台 JWT 解析和认证 SDK。
  - 未登录用户通过 HMAC 签名 Cookie 获得稳定匿名归属。
  - 登出后清理会话状态，防止旧会话继续访问或提交任务。

- **项目与版本管理**
  - 管理项目、版本、生成步骤和消息记录。
  - 支持项目列表、版本切换、历史内容恢复和回滚。
  - 使用版本更新时间和内容哈希判断最新版本及恢复结果。
  - 演示项目在界面中明确展示只读状态。

- **工作区**
  - 三栏布局展示项目列表、智能体步骤与对话、代码及应用预览。
  - 支持步骤状态展示、步骤回放、暂停、重置、停止和恢复生成。
  - 展示 `pending`、`running`、`succeeded`、`failed`、`cancelled` 等状态。
  - 生成运行期间禁用冲突操作并展示失败原因。

- **沙箱预览**
  - 提取完整 HTML，校验文档结构并注入 CSP 后通过 iframe 渲染。
  - iframe 使用 `allow-scripts allow-forms`，不授予 `allow-same-origin`。
  - 源码查看器和预览使用一致的最终 HTML 内容。
  - 对截断、缺少文档结构和无效模型输出提供明确错误。

- **后端 API**
  - 提供认证、项目、版本、消息、生成步骤、设置、存储及 AI Hub 接口。
  - 支持步骤查询、版本恢复、生成取消、归属过滤和任务恢复。
  - 统一处理参数校验、资源不存在、权限隔离、演示项目只读、生成失败和取消冲突。
  - 步骤接口返回 `error_type` 等前端所需字段。

- **数据与迁移**
  - 使用 ORM 模型管理业务实体及项目归属字段。
  - 使用 Alembic 管理数据库迁移。
  - 提供幂等演示项目回填脚本。
  - 后台生成任务使用独立数据库会话。
  - 提供 mock 数据、数据模型 JSON 和生成生命周期契约文档。

- **自动化测试与验收**
  - HTML 提取、上下文记忆、归属隔离、版本恢复和流水线恢复测试。
  - 覆盖失败/取消轮次历史标注、成功 HTML 基线保护、指代消解和两轮增量。
  - 覆盖 stale 回收、缺失时间戳、本地 naive 时间、实时取消和迟到成功响应。
  - 使用 FakeAIHub 验证异步生成行为。
  - 后端全量 157 项测试通过，前端 lint、`tsc --noEmit` 和 build 通过。

# Directory Tree

```text
app/
├── .mgx/
│   └── config.yaml                                  # 后端工程配置
├── backend/
│   ├── main.py                                      # FastAPI 应用入口
│   ├── lambda_handler.py                            # Serverless 处理入口
│   ├── e2e_acceptance.py                             # 端到端验收脚本
│   ├── e2e_two_round.py                              # 两轮增量端到端验证
│   ├── probe_upstream.py                             # 上游 AI 能力探测脚本
│   ├── requirements.txt                              # 后端依赖
│   ├── requirements.default                          # 默认依赖模板
│   ├── pytest.ini                                    # pytest 测试配置
│   ├── alembic.ini                                   # 数据库迁移配置
│   ├── alembic/                                      # Alembic 环境和迁移版本
│   ├── core/                                         # 配置、认证、数据库、枚举和遥测
│   ├── dependencies/
│   │   └── owner.py                                  # 用户及匿名访客归属解析
│   ├── middlewares/                                  # FastAPI 中间件
│   ├── models/                                       # ORM 数据模型
│   ├── schemas/                                      # API 请求与响应结构
│   ├── routers/
│   │   └── atoms.py                                  # 生成、历史查询、步骤、恢复和取消接口
│   ├── services/
│   │   ├── pipeline.py                               # 生成、恢复、增量、预算和 stale 控制
│   │   ├── prompts.py                                # 历史需求、状态标注和指代提示词
│   │   ├── aihub.py                                  # AI Hub 调用封装
│   │   ├── aihub_errors.py                            # AI Hub 错误分类
│   │   ├── html_extract.py                            # HTML 提取、校验和 CSP 注入
│   │   └── *.py                                      # 项目、版本、消息、步骤等业务服务
│   ├── data_models/                                  # 数据结构定义 JSON
│   ├── mock_data/                                    # 演示种子数据
│   ├── scripts/
│   │   └── backfill_demo_owner.py                    # 演示项目归属回填脚本
│   ├── skills_docs/                                  # AI、对象存储和 SDK 文档
│   ├── tests/
│   │   ├── conftest.py                               # 测试环境、会话和数据库夹具
│   │   ├── fakes.py                                  # FakeAIHub 等测试替身
│   │   ├── test_html_extract.py                      # HTML 处理测试
│   │   ├── test_context_memory.py                    # 上下文记忆与续写测试
│   │   ├── test_owner_isolation.py                   # 归属隔离测试
│   │   ├── test_owner_dependency.py                  # Owner 依赖测试
│   │   ├── test_pipeline_recovery.py                 # 流水线恢复测试
│   │   ├── test_stale_recovery.py                    # stale 任务和时间口径测试
│   │   ├── test_two_round_increment.py               # 两轮增量和失败历史测试
│   │   ├── test_budget_exhaustion.py                 # 预算耗尽测试
│   │   ├── test_cancel_live_task.py                  # 实时任务取消测试
│   │   ├── test_generation_inline.py                 # 内联生成测试
│   │   ├── test_fake_aihub.py                        # AI Hub 测试替身测试
│   │   ├── test_session_logout.py                    # 会话登出测试
│   │   ├── test_source_preview_consistency.py        # 源码与预览一致性测试
│   │   ├── test_time_budget_invariants.py             # 时间预算不变量测试
│   │   ├── test_conftest_smoke.py                    # 测试夹具冒烟测试
│   │   └── test_version_restore.py                   # 版本恢复测试
│   └── utils/                                        # 日志等通用工具
├── frontend/
│   ├── package.json                                  # 前端依赖与脚本
│   ├── vite.config.ts                                # Vite 配置
│   ├── tailwind.config.ts                            # Tailwind 配置
│   ├── index.html                                    # Vite 前端 HTML 入口
│   ├── src/
│   │   ├── App.tsx                                   # 应用路由、认证门控与整体布局
│   │   ├── main.tsx                                  # React 启动入口
│   │   ├── pages/                                    # 工作区、认证和博客页面
│   │   ├── components/                               # 工作区、预览、对话和项目组件
│   │   ├── components/ui/                            # 通用 UI 组件
│   │   ├── contexts/
│   │   │   └── AuthContext.tsx                       # 认证状态和用户会话
│   │   ├── hooks/                                    # React Hooks
│   │   ├── lib/
│   │   │   ├── atoms.ts                              # 生成、轮询、恢复和取消 API
│   │   │   ├── auth.ts                               # 登录、登出和认证请求
│   │   │   └── constants.ts                          # 前端常量
│   │   ├── api/                                      # 设置接口封装
│   │   └── ...
│   ├── public/                                       # 静态资源
│   └── prerender/                                   # 博客预渲染与站点地图生成
├── CLAUDE.md                                         # AI 协作与项目开发约定
├── docs/
│   ├── superpowers/plans/                            # 实施计划文档
│   ├── superpowers/specs/                            # 技术规格文档
│   ├── 优化文档-无登录数据隔离与生成链路修复.md       # 无登录隔离和生成链路优化说明
│   └── 验收复核报告-生成流水线与归属隔离.md            # 生成流水线与归属隔离验收报告
├── start_app_v2.sh                                   # 应用启动脚本
└── uploads/
    ├── specs/001-atoms-demo/                         # 首期 Demo 需求和设计文档
    ├── specs/002-harden-increment-session/           # 增量会话加固规格、契约、证据和任务
    └── tasks (1).md                                  # 项目任务记录
```

# File Description Inventory

| 范围 | 主要文件 | 用途 |
|---|---|---|
| 归属解析 | `backend/dependencies/owner.py` | 解析 JWT、匿名身份和 OwnerContext |
| 生成路由 | `backend/routers/atoms.py` | 接收生成请求、查询带状态的历史、步骤轮询、恢复和取消任务 |
| 生成流水线 | `backend/services/pipeline.py` | 执行多阶段生成、增量处理、预算控制、恢复、心跳和 stale 控制 |
| AI 调用 | `backend/services/aihub.py`、`aihub_errors.py` | 调用上游 AI 服务并分类异常 |
| 生成提示词 | `backend/services/prompts.py` | 拼接成功及失败历史、处理指代并保护成功 HTML 基线 |
| HTML 处理 | `backend/services/html_extract.py` | 提取完整 HTML、校验结构、处理截断并注入 CSP |
| 归属回填 | `backend/scripts/backfill_demo_owner.py` | 将无归属历史项目幂等标记为只读演示项目 |
| 前端认证 | `frontend/src/contexts/AuthContext.tsx`、`frontend/src/lib/auth.ts` | 管理用户会话、登录、登出和认证状态 |
| 前端 API | `frontend/src/lib/atoms.ts` | 封装生成受理、步骤轮询、版本恢复和取消请求 |
| 主工作区 | `frontend/src/pages/Index.tsx` | 管理生成轮询、恢复、停止、预算错误和失败展示 |
| 需求输入 | `frontend/src/components/PromptInput.tsx` | 输入需求并根据任务状态控制提交或停止 |
| 版本管理 | `frontend/src/components/VersionSwitcher.tsx` | 切换、恢复版本并展示演示项目只读状态 |
| 源码查看 | `frontend/src/components/CodeViewer.tsx` | 展示生成源码并保持与预览内容一致 |
| 数据访问 | `backend/models/*.py`、`backend/services/database.py` | 定义实体、归属字段和数据库操作 |
| 测试基础设施 | `backend/tests/conftest.py`、`backend/tests/fakes.py` | 提供 SQLite、认证、会话和 FakeAIHub 测试支持 |
| 历史记忆测试 | `backend/tests/test_two_round_increment.py` | 验证失败/取消历史标注、指代消解和成功基线保护 |
| 流水线测试 | `backend/tests/test_pipeline_recovery.py`、`test_stale_recovery.py` | 验证中断恢复、心跳、时间口径和过期任务修复 |
| 生命周期测试 | `backend/tests/test_cancel_live_task.py`、`test_budget_exhaustion.py`、`test_time_budget_invariants.py` | 验证取消、预算耗尽和时间预算约束 |
| 端到端验证 | `backend/e2e_acceptance.py`、`backend/e2e_two_round.py` | 执行完整链路及双轮增量验收 |
| 项目文档 | `docs/`、`uploads/specs/002-harden-increment-session/` | 记录设计、契约、验收证据和实施任务 |

# Technology Stack

- **后端**：Python、FastAPI、SQLAlchemy、Alembic、Pydantic、asyncio 后台任务
- **AI 能力**：AI Hub、DeepSeek、多阶段需求分析与 HTML 代码生成
- **前端**：React、TypeScript、Vite
- **样式与组件**：Tailwind CSS、shadcn 风格 UI、PostCSS
- **应用预览**：受 CSP 约束的 sandbox iframe
- **数据存储**：关系型数据库、ORM、Alembic、对象存储
- **认证安全**：JWT、HMAC 匿名 Cookie、认证 SDK、受保护路由
- **任务控制**：数据库任务状态、后台任务、会话生命周期、预算、取消、心跳、恢复和 stale 检测
- **时间处理**：数据库本地 naive 时间戳统一比较，缺失时间戳保守跳过 stale 回收
- **上下文管理**：历史需求状态标注、指代消解、版本摘要和增量上下文
- **版本一致性**：版本更新时间、回滚恢复、SHA-256 内容校验、源码预览一致性
- **输出可靠性**：HTML 完整性校验、截断续写、整篇重跑和错误分类
- **测试与质量**：pytest、SQLite 测试库、FakeAIHub、端到端验收、ESLint、TypeScript 构建检查
- **部署入口**：Uvicorn 开发运行、Serverless Lambda 入口

# Usage

## 后端安装与运行

```bash
cd app/backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
alembic upgrade head
uvicorn main:app --reload
```

Windows 激活虚拟环境：

```bash
.venv\Scripts\activate
```

根据运行环境配置数据库、JWT、AI Hub、对象存储等必要环境变量。测试环境需提供 JWT 相关配置；首次运行时可按项目需要加载 `mock_data` 中的演示数据。

## 演示项目归属回填

```bash
cd app/backend
python scripts/backfill_demo_owner.py
```

脚本会将无 `owner_key` 的历史项目幂等标记为 `is_demo=true`，并保持演示项目只读。

## 前端安装与运行

```bash
cd app/frontend
npm install
npm run dev
```

## 前端检查与构建

```bash
npm run lint
npx tsc --noEmit
npm run build
```

## 后端测试

```bash
cd app/backend
pytest
```

## 端到端验证

```bash
cd app/backend
python e2e_acceptance.py
python e2e_two_round.py
```

运行端到端验证前，需要准备对应的数据库、认证和 AI Hub 环境变量。

## 一键启动

```bash
cd app
bash start_app_v2.sh
```

启动后可体验项目归属隔离、演示项目只读、异步生成、历史需求迭代、失败重试、两轮增量、预算控制、实时取消、stale 任务恢复、版本回滚、对话和沙箱预览。
