# Project Summary

Atoms Studio 是一个面向应用原型和业务页面生成的智能应用开发平台。用户通过自然语言描述需求，系统完成需求分析、结构设计和 HTML 代码生成，并在隔离沙箱中预览可交互应用。项目、版本、生成步骤及对话记录会持久化保存，支持基于历史版本继续迭代。

生成流程采用异步任务模式：请求完成校验并预创建版本、步骤后立即返回，后台执行三阶段 AI 生成；前端通过轮询步骤状态获取进度，并在完成后加载结果。代码生成阶段现支持截断续写、整篇重跑兜底和 HTML 完整性校验，避免内容密集型页面产生残缺结果。

当前 Demo 已完成异步生成、步骤轮询、失败提示、代码截断续写、版本切换、步骤回放、对话记录、沙箱预览及自动化验证。

# Project Module Description

- **异步智能生成流水线**
  - 通过 AI Hub 执行需求分析、结构设计和 HTML 代码生成。
  - 生成接口预处理后返回 `202 Accepted`，后台任务使用独立数据库会话执行。
  - 前端轮询步骤状态，生成成功后加载版本详情；支持失败状态和中文错误提示。
  - 代码生成被截断且缺少 `</html>` 时，携带已生成内容要求模型从断点续写，最多续写两轮。
  - 续写仍不完整时自动整篇重跑一次。
  - 提示词要求使用 JavaScript 数组渲染数据，避免重复硬编码 HTML，降低超长输出概率。
- **项目与版本管理**
  - 管理项目、版本、生成步骤和消息记录。
  - 支持项目列表、版本切换和历史内容恢复。
  - 打开项目时可自动接回进行中的生成任务。
  - 已包含演示数据：3 个项目、4 个版本、8 条消息、12 个步骤。
- **工作区**
  - 三栏布局展示项目列表、智能体步骤与对话、代码及应用预览。
  - 支持步骤状态展示、步骤回放、暂停和重置。
  - 生成期间持续反映 `pending`、`running`、`succeeded`、`failed` 等状态。
- **沙箱预览**
  - 自动提取生成的完整 HTML，校验文档结构并注入 CSP 后通过 iframe 渲染。
  - iframe 使用 `allow-scripts allow-forms`，不授予 `allow-same-origin`。
- **认证与用户**
  - 提供认证回调、登出回调、受保护路由及用户接口。
- **后端 API**
  - 提供健康检查、认证、项目、版本、消息、生成步骤、设置、存储及 AI Hub 接口。
  - 提供版本步骤查询接口，供前端轮询后台任务进度。
  - 统一处理参数校验、资源不存在、生成失败和 HTML 不完整响应。
- **数据与迁移**
  - 使用 ORM 模型管理业务实体。
  - 使用 Alembic 管理数据库迁移。
  - 后台生成任务为每次执行创建独立数据库会话。
  - 提供 mock 数据和数据模型 JSON 文件。
- **自动化测试**
  - `test_html_extract.py` 覆盖 HTML 提取、截断校验、CSP 注入和错误场景等 17 项测试。
  - 已通过后端测试、前端 lint、生产构建及真实端到端生成验证。

# Directory Tree

```text
app/
├── .mgx/
│   └── config.yaml                 # 后端工程配置
├── backend/
│   ├── main.py                     # FastAPI 应用入口
│   ├── lambda_handler.py           # Serverless 处理入口
│   ├── requirements.txt            # 后端依赖
│   ├── requirements.default        # 默认依赖模板
│   ├── alembic.ini                 # 数据库迁移配置
│   ├── alembic/                    # Alembic 环境和迁移版本
│   ├── core/                       # 配置、认证、数据库、枚举和遥测
│   ├── dependencies/               # 认证与数据库依赖注入
│   ├── middlewares/                # FastAPI 中间件
│   ├── models/                     # ORM 数据模型
│   ├── schemas/                    # API 请求与响应结构
│   ├── routers/                    # REST API 路由
│   ├── services/
│   │   ├── pipeline.py             # 三阶段生成、截断续写和重跑兜底
│   │   ├── prompts.py              # AI 生成提示词与输出约束
│   │   ├── aihub.py                # AI Hub 调用封装
│   │   ├── html_extract.py         # HTML 提取、校验和 CSP 注入
│   │   └── *.py                    # 项目、版本、消息、步骤等业务服务
│   ├── data_models/                # 数据结构定义 JSON
│   ├── mock_data/                  # 演示种子数据
│   ├── skills_docs/                # AI、对象存储和 SDK 文档
│   ├── tests/                      # 后端自动化测试
│   └── utils/                      # 日志等通用工具
├── frontend/
│   ├── package.json                # 前端依赖与脚本
│   ├── vite.config.ts              # Vite 配置
│   ├── tailwind.config.ts          # Tailwind 配置
│   ├── index.html                  # 前端 HTML 入口
│   ├── src/
│   │   ├── App.tsx                 # 应用路由与整体布局
│   │   ├── main.tsx                # React 启动入口
│   │   ├── pages/                  # 工作区、认证和博客页面
│   │   ├── components/             # 工作区、预览、对话和项目组件
│   │   ├── components/ui/          # 通用 UI 组件
│   │   ├── contexts/               # 认证上下文
│   │   ├── hooks/                  # React Hooks
│   │   ├── lib/                    # API、认证、配置和工具函数
│   │   └── api/                    # 设置接口封装
│   ├── public/                     # 静态资源
│   └── prerender/                  # 博客预渲染与站点地图生成
├── start_app_v2.sh                 # 应用启动脚本
└── uploads/
    ├── specs/001-atoms-demo/       # Demo 需求、接口和实施文档
    └── tasks.md                    # 项目任务记录
```

# File Description Inventory

| 范围 | 主要文件 | 用途 |
|---|---|---|
| 后端入口 | `backend/main.py`、`lambda_handler.py` | 创建 Web 应用及提供 Serverless 入口 |
| 生成路由 | `backend/routers/atoms.py` | 接收生成请求、预创建版本和步骤，并提供步骤查询 |
| 生成流水线 | `backend/services/pipeline.py` | 执行三阶段生成、截断续写、重跑和状态管理 |
| 生成提示词 | `backend/services/prompts.py` | 定义需求分析、结构设计和 HTML 输出约束 |
| AI 调用 | `backend/services/aihub.py` | 封装 AI Hub 请求 |
| 核心基础设施 | `backend/core/*.py` | 环境配置、数据库、认证、加密和请求遥测 |
| 数据访问 | `backend/models/*.py`、`backend/services/database.py` | 定义实体并封装数据库操作 |
| 内容处理 | `backend/services/html_extract.py`、`storage.py` | 提取完整 HTML、校验文档结构、注入 CSP 并处理存储 |
| 业务服务 | `services/projects.py`、`versions.py`、`messages.py`、`generation_steps.py`、`user.py` | 管理项目、版本、消息、步骤和用户 |
| 数据资源 | `data_models/*.json`、`mock_data/*.json` | 定义数据结构及初始化演示数据 |
| 前端 API | `frontend/src/lib/atoms.ts` | 封装生成受理、步骤轮询和版本查询 |
| 前端页面 | `frontend/src/pages/Index.tsx` 及认证、博客页面 | 提供工作区、认证和异步生成恢复 |
| 前端工作区 | `AgentSteps.tsx`、`ConversationPanel.tsx`、`PreviewPane.tsx`、`CodeViewer.tsx` | 展示进度、对话、代码和沙箱预览 |
| 前端交互 | `PromptInput.tsx`、`ProjectList.tsx`、`VersionSwitcher.tsx` | 输入需求、选择项目和切换版本 |
| 测试 | `backend/tests/test_html_extract.py` | 验证 HTML 提取、完整性和 CSP 处理逻辑 |
| 项目文档 | `uploads/specs/001-atoms-demo/*`、`skills_docs/*` | 记录需求、接口、实施计划和能力说明 |

# Technology Stack

- **后端**：Python、FastAPI、SQLAlchemy、Alembic、Pydantic、asyncio 后台任务
- **AI 能力**：AI Hub、DeepSeek，多阶段需求分析与代码生成
- **前端**：React、TypeScript、Vite
- **样式与组件**：Tailwind CSS、shadcn 风格 UI、PostCSS
- **应用预览**：受 CSP 约束的 sandbox iframe
- **数据存储**：关系型数据库、ORM、Alembic、对象存储
- **认证安全**：认证依赖、回调页面、加密及受保护路由
- **任务进度**：数据库持久化步骤状态、前端定时轮询和刷新恢复
- **输出可靠性**：HTML 完整性校验、截断续写、整篇重跑兜底
- **测试与质量**：pytest、ESLint、TypeScript 构建检查、端到端验证
- **部署入口**：Uvicorn 开发运行、Serverless Lambda 入口

# Usage

## 后端安装与运行

```bash
cd app/backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
alembic upgrade head
uvicorn main:app --reload
```

Windows 激活虚拟环境：

```bash
.venv\Scripts\activate
```

根据运行环境配置数据库、认证、AI Hub、对象存储等必要环境变量。首次运行时按项目需要加载 `mock_data` 中的演示数据。

## 前端安装与运行

```bash
cd app/frontend
npm install
npm run dev
```

## 前端检查与构建

```bash
npm run lint
npm run build
```

## 后端测试

```bash
cd app/backend
pytest
```

## 一键启动

```bash
cd app
bash start_app_v2.sh
```

启动后可进入 App Viewer，选择项目体验项目列表、异步生成、步骤轮询、对话、版本切换和沙箱预览。内容密集型需求生成时，代码阶段会自动处理截断续写，并在保存前校验完整 HTML。
